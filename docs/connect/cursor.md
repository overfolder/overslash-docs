---
title: Cursor
---

# Cursor

Cursor consumes the same standard `.mcp.json` config format as Claude Code. Add an `overslash` entry with `"type": "http"` and the URL of your Overslash instance; on first use, Cursor opens a browser for the OAuth flow.

## Configure `.mcp.json`

## Verifying the connection

## Renaming or revoking the agent

<!-- TODO: see FOLLOW_UPS.md -->
